---
tags: ["markdown", "mdx"]
---

TIL:  
MDX 是 Markdown 格式的扩展，支持在 md 文件中加入 JSX 语法（React 组件），在 Markdown 基础上，MDX 支持了 `<>` 和 `{}` 两组符号，因此，对于出现 `<` 和 `{` 的地方，需要转义（`\<`, `\{`），否则会被当成 JSX 组件，`>` 和 `}` 则不用转义。在代码块中的 `<` 和 `{` 也不用转义。
